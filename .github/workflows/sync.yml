        run: |
          # 1. 强力删除那些报错的超大垃圾缓存文件夹
          rm -rf .serena
          rm -rf apps/shared/ClawdisKit/.build
          
          # 2. 配置身份（防止报错）
          git config --global user.email "echo8900@example.com"
          git config --global user.name "echo8900"
          
          # 3. 把删除动作提交了
          git add .
          git commit -m "Clean up large cache files" || echo "No changes to commit"
          
          # 4. 强制推送到 HF
          git remote add hf https://echo8900:$HF_TOKEN@huggingface.co/spaces/echo8900/claw
          git push --force hf main
